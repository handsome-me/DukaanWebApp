import React from 'react';
import PropTypes from 'prop-types';

const Space = props => {
    return (
        <div style={{margin:"1.5rem"}}>
            
        </div>
    );
};

Space.propTypes = {
    
};

export default Space;